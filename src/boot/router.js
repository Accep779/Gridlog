import { boot } from 'quasar/wrappers'

export default boot(({ router, app }) => {
  // Router guards are configured in router/index.js
  // This boot file ensures router is initialized before app mount
})
